# Upstash Redis Configuration
# This is used for the webhook feature to store chat messages
# You can use either Upstash Redis directly or Vercel KV (which is powered by Upstash)
#
# Option 1: Upstash Redis (Recommended)
# Get these from: https://console.upstash.com/
# UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your_upstash_token_here
#
# Option 2: Vercel KV (Legacy)
# These are automatically set when you connect Vercel KV to your project
# KV_REST_API_URL=https://your-kv-instance.kv.vercel-storage.com
# KV_REST_API_TOKEN=your_vercel_kv_token_here
#
# NOTE: The @vercel/kv SDK automatically detects and uses either set of variables

# Google Sheets Configuration
# The URL should be a published Google Sheet in CSV format
#
# Required Columns in your Google Sheet:
# - id: Unique identifier for the user
# - username: User's login username
# - password: User's password (plain text for now)
# - modules: Comma-separated list of modules user has access to
#            Available modules: invoice, kdr, ga, kdr invoicing
#            Example: "invoice,kdr,ga" or "invoice,kdr invoicing"
#
# Optional Columns:
# - role: User role (optional)
# - email: User email (optional)
#
# To get this URL:
# 1. Open your Google Sheet
# 2. Go to File > Share > Publish to web
# 3. Select "Entire Document" and "Comma-separated values (.csv)"
# 4. Copy the generated URL
# 5. Paste it below
#
# IMPORTANT FOR VERCEL DEPLOYMENT:
# You need to set BOTH variables in Vercel:
# - VITE_GOOGLE_SHEET_URL (for frontend, available at build time)
# - GOOGLE_SHEET_URL (for backend API, available at runtime)
# Both should have the SAME value (your Google Sheets CSV URL)

# For local development:
VITE_GOOGLE_SHEET_URL=https://docs.google.com/spreadsheets/d/e/2PACX-1vR21vntA5bTAeUWpzEUdGEXLmFUMqjH5LRUT5uPxmq3ipaHWqndB65-dli_kcmlw-jQKgu7Z6ERGeMh/pub?output=csv

# For Vercel serverless functions (backend API):
# Note: This is automatically available in Vercel if you set it in project settings
GOOGLE_SHEET_URL=https://docs.google.com/spreadsheets/d/e/2PACX-1vR21vntA5bTAeUWpzEUdGEXLmFUMqjH5LRUT5uPxmq3ipaHWqndB65-dli_kcmlw-jQKgu7Z6ERGeMh/pub?output=csv

# n8n Webhook URLs
# These are the webhook URLs from your n8n workflows for each processing module
#
# To get these URLs:
# 1. Create n8n workflows for each module
# 2. Add a Webhook trigger node to each workflow
# 3. Copy the production webhook URLs
# 4. Paste them below
#
# Note: These URLs should be accessible from your frontend (CORS must be enabled in n8n)

# Chat module (AI Chat Assistant with multiple async responses)
VITE_N8N_WEBHOOK_URL=https://your-n8n-instance.app.n8n.cloud/webhook/chat

# Invoice Processing module
VITE_N8N_INVOICE_WEBHOOK_URL=https://n8n.srv1009033.hstgr.cloud/webhook/invoice

# KDR Processing module
VITE_N8N_KDR_WEBHOOK_URL=https://n8n.srv1009033.hstgr.cloud/webhook/kdr

# GA Processing module
VITE_N8N_GA_WEBHOOK_URL=https://n8n.srv1009033.hstgr.cloud/webhook/ga

# KDR Invoicing module
VITE_N8N_KDR_INVOICING_WEBHOOK_URL=https://n8n.srv1009033.hstgr.cloud/webhook/KDRprocessing
